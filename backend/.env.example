# Server Configuration
NODE_ENV=development
PORT=3001

# Database
DATABASE_URL=postgresql://schedula:schedula_dev_password@db:5432/schedula_db

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_ACCESS_SECRET=dev_access_secret_change_in_production
JWT_REFRESH_SECRET=dev_refresh_secret_change_in_production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Email Configuration
# Leave these commented out to use console-only mode (emails logged to backend console)
# Uncomment and configure to send real emails via Gmail SMTP
# Note: For Gmail, use an App Password, not your regular password
# Generate at: https://myaccount.google.com/apppasswords
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USE_TLS=True
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password
# DEFAULT_FROM_EMAIL="Schedula <your_email@gmail.com>"
